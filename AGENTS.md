## Agent skills

### Issue tracker

Issues and specifications are tracked as local Markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repository. Read the root `CONTEXT.md` and relevant decisions under `docs/adr/` when they exist. See `docs/agents/domain.md`.
